#include <stdio.h>

int main() {
    int a = 10, b = 8;
    int product, sum;

    product = a * b;
    sum = 10 + 8;

    printf("Product = %d\n", product);
    printf("Sum = %d\n", sum);

    return 0;
}
